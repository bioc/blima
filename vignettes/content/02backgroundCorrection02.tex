The blima package also contain methods for standard background correction. We can use the function \Rfunction{backgroundChannelSubtract} for background subtraction and \Rfunction{xieBacgroundCorrect} for RMA-like background convolution. If we want to create slot "BGS" for the graphical background subtracted data (subtract "GrnF - GrnB") we may call 