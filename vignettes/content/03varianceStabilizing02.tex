If we do like to perform log2 transformation instead of variance stabilization for example of the slot "GrnF" we can call function \Rfunction{selectedChannelTransform} with transformation parameter set to \Rfunction{log2TransformPositive} we call the result "LOG"