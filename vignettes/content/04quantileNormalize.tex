\section{Quantile normalization}
In the next step we perform the quantile normalization of the bead level data. We use the bead level quantile normalization algorithm implemented in the function \Rfunction{quantileNormalize}.